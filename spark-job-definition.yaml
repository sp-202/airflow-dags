apiVersion: "sparkoperator.k8s.io/v1beta2"
kind: SparkApplication
metadata:
  name: "dri-ingest-final-fix"
  namespace: default
spec:
  type: Python
  pythonVersion: "3"
  mode: cluster
  image: "subhodeep2022/spark-bigdata:spark-4.0.1-uc-0.3.1-fix-v4-mssql-opt"
  mainApplicationFile: "local:///app/ingest_dri_data.py"
  sparkVersion: "4.0.1"

  # Keep for backup, but the sparkConf below is the primary fix
  sparkConfigMap: "spark-config" 

  sparkConf:
    # --- 1. CRITICAL HADOOP FIXES (Plain integers to avoid NumberFormatException) ---
    "spark.hadoop.fs.s3a.threads.keepalivetime": "60"
    "spark.hadoop.fs.s3a.connection.timeout": "20000"
    "spark.hadoop.fs.s3a.connection.establish.timeout": "30000"
    "spark.hadoop.fs.s3a.connection.acquisition.timeout": "60000"
    "spark.hadoop.fs.s3a.connection.idle.time": "60000"
    "spark.hadoop.fs.s3a.connection.request.timeout": "60000"
    "spark.hadoop.fs.s3a.connection.ttl": "300000"
    "spark.hadoop.fs.s3a.multipart.purge.age": "86400"
    "spark.hadoop.fs.s3a.paging.maximum": "5000"
    
    # --- 2. NETWORKING & TIMEOUTS ---
    "spark.network.timeout": "120"
    "spark.files.fetchTimeout": "60"
    "spark.executor.heartbeatInterval": "10"
    "spark.rpc.askTimeout": "120"

    # --- 3. SQL & DELTA CONFIGS ---
    "spark.sql.ansi.enabled": "false"
    "spark.sql.extensions": "io.delta.sql.DeltaSparkSessionExtension"
    "spark.sql.catalog.spark_catalog": "org.apache.spark.sql.delta.catalog.DeltaCatalog"
    "spark.sql.catalogImplementation": "hive"
    "spark.databricks.delta.universalFormat.enabledFormats": "none"
    "spark.databricks.delta.allowArbitraryProperties.enabled": "true"

    # --- 4. S3A IMPLEMENTATION ---
    "spark.hadoop.fs.s3a.impl": "org.apache.hadoop.fs.s3a.S3AFileSystem"
    "spark.hadoop.fs.s3a.path.style.access": "true"
    "spark.hadoop.fs.s3a.connection.ssl.enabled": "false"
    "spark.hadoop.fs.s3a.aws.credentials.provider": "org.apache.hadoop.fs.s3a.SimpleAWSCredentialsProvider"

    # --- 5. PVC MOUNTS (Scripts) ---
    "spark.kubernetes.driver.volumes.persistentVolumeClaim.dags-shared.options.claimName": "airflow-dags-pvc"
    "spark.kubernetes.driver.volumes.persistentVolumeClaim.dags-shared.mount.path": "/app"
    "spark.kubernetes.driver.volumes.persistentVolumeClaim.dags-shared.mount.subPath": "repo/scripts"
    "spark.kubernetes.executor.volumes.persistentVolumeClaim.dags-shared.options.claimName": "airflow-dags-pvc"
    "spark.kubernetes.executor.volumes.persistentVolumeClaim.dags-shared.mount.path": "/app"
    "spark.kubernetes.executor.volumes.persistentVolumeClaim.dags-shared.mount.subPath": "repo/scripts"

  driver:
    cores: 1
    memory: "1g"
    serviceAccount: spark-operator-spark
    # FORCE SPARK TO IGNORE BROKEN XML FILES IN THE IMAGE
    env:
      - name: HADOOP_CONF_DIR
        value: "/tmp" 
    volumeMounts:
      - name: spark-defaults-vol
        mountPath: /opt/spark/conf/spark.properties
        subPath: spark-defaults.conf
  
  executor:
    cores: 2
    instances: 1
    memory: "4g"
    env:
      - name: HADOOP_CONF_DIR
        value: "/tmp"
    volumeMounts:
      - name: spark-defaults-vol
        mountPath: /opt/spark/conf/spark.properties
        subPath: spark-defaults.conf

  volumes:
    - name: spark-defaults-vol
      configMap:
        name: spark-config
    - name: dags-shared
      persistentVolumeClaim:
        claimName: airflow-dags-pvc